# CV Generator dependencies
python-docx==1.1.2
docxtpl==0.18.0
openpyxl==3.1.5
Pillow==10.4.0
rapidocr-onnxruntime==1.3.24
numpy==1.26.4
PyMuPDF==1.24.10
streamlit==1.39.0
# Windows-only: DOCX->PDF via Microsoft Word. On Linux/cloud, LibreOffice is
# used instead (install it via packages.txt / Dockerfile).
pywin32==306; sys_platform == "win32"
docx2pdf==0.1.8; sys_platform == "win32"
